import flags from 'guess-the-flag-with-friends/utils/flags';
import { module, test } from 'qunit';

module('Unit | Utility | flags');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flags();
  assert.ok(result);
});
