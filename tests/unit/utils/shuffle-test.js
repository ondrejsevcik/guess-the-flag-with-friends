import shuffle from 'guess-the-flag-with-friends/utils/shuffle';
import { module, test } from 'qunit';

module('Unit | Utility | shuffle');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shuffle();
  assert.ok(result);
});
