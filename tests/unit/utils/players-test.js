import players from 'guess-the-flag-with-friends/utils/players';
import { module, test } from 'qunit';

module('Unit | Utility | players');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = players();
  assert.ok(result);
});
